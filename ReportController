package com.example.payment.controller; import org.springframework.web.bind.annotation.*; import java.util.*; import com.example.payment.dto.*; import com.example.payment.service.*; @RestController @RequestMapping("/api/reports") @CrossOrigin public class ReportController{ private final ReportService s; public ReportController(ReportService s){this.s=s;} @GetMapping("/status") public List<StatusCountDto> status(){ return s.statusReport(); }}